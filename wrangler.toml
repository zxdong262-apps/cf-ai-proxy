name = "ai"
compatibility_date = "2024-01-01"
main = "src/worker.js"
compatibility_flags = ["nodejs_compat"]

# Only your account ID is required so the worker can build the correct URL.
# [vars]
# CF_ACCOUNT_ID = "your-cloudflare-account-id"
